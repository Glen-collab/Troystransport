import React from 'react';
import ReactDOM from 'react-dom/client';
import TruckingDispatchBoard from './dispatch-board';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TruckingDispatchBoard />
  </React.StrictMode>
);
